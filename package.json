{
  "name": "org-ietf-ospf",
  "private": true,
  "description": "OSPFv2 (RFC 2328) packet and LSA-header wire codec in portable .cljc",
  "scripts": {
    "test:cljs": "nbb --classpath \"$(clojure -A:cljs -Spath)\" scripts/verify-cljs.cljs"
  }
}
